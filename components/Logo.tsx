import React, { FC } from "react";

export const Logo: FC = () => {
  return (
    <div
      style={{
        fontWeight: "bold",
        fontFamily: "sans-serif",
        fontSize: "30px",
      }}
    >
      BLOG SAMPLE
    </div>
  );
};
